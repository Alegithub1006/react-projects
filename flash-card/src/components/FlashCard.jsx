import { useState } from "react";

export function FlashCard({ flashcardData }) {
    const [currentIndex, setCurrentIndex]= useState(0)
    const curretntFlashcard = flashcardData[currentIndex]
  return (
    <>
      
    </>
  );
}
